# 기술 부채 카탈로그

> 고치는 방법은 여기 없다. 리팩토링 단계에서 같은 표에 열을 덧붙인다.
> 처방이 비어 있는 것은 의도다.

`당시엔 왜` 는 개념 부재 / 관행 둘로 표기한다. 둘 다 해당되면 함께 적었다.

> 일정 압박은 유형으로 두지 않았다. 촉박하든 여유롭든 코드의 모양을 정하는 것은
> 그때 알던 것과 하던 방식이지 일정이 아니다.
> 일정은 얼마나 많이 하느냐를 바꿀 뿐, 어떻게 짜느냐를 바꾸지 않는다.

---

## 1. 데이터 · 쿼리

| 부채 | 증상 | 당시엔 왜 |
|---|---|---|
| 관리자 검색이 전부 `LIKE '%키워드%'`<br>주문관리·상품관리 | 앞 와일드카드라 인덱스로 못 푼다. 인덱스를 아무리 걸어도 풀스캔이다 | 개념 부재 |
| 걸러내지 못하는 조회 조건<br>`PRD_STAT_CD IN ('30','40','50')` | 전체의 95% 가 통과한다. 조건문이 있으나 마나 | 개념 부재 |
| 매 요청마다 다시 하는 정렬 | 12건만 보여줄 화면인데 매 요청마다 전량을 정렬한다 | 개념 부재 |
| 큰 `OFFSET` 페이징 | `OFFSET N` 은 건너뛰기가 아니다. N+12 건을 만들어놓고 N 건을 버린다 | 개념 부재 |
| 조인한 다른 테이블 컬럼 기준 정렬<br>가격순 정렬 기준이 `TB_PRD_PRC` 에 있다 | `LIMIT 12` 를 걸어도 전량을 조인한 뒤에야 정렬할 수 있다 | 개념 부재 |
| SQL 에 직접 박은 벤더 함수 | DB 를 바꾸는 순간 그 함수를 쓴 쿼리를 전부 찾아 고쳐야 한다 | 관행 |

### 인덱스는 있긴하다.

조인 컬럼과 주문 조회에 인덱스가 존재한다.

```sql
KEY IX_PRD_IMG_01  (PRD_CD, IMG_TP_CD, SORT_SEQ)   -- 목록 조회 조인
KEY IX_ORD_MST_01  (MBR_NO, ORD_DT)                -- 마이페이지 주문내역
KEY IX_ORD_MST_02  (ORD_DT)                        -- 관리자 기간검색
KEY IX_CART_01     (MBR_NO)                        -- 장바구니
KEY IX_ORD_TEMP_01 (MBR_NO)                        -- 임시주문 조회
```

그런데도 위 표의 항목들은 그대로 남는다. 인덱스로 풀리는 종류가 아니기 때문이다.

측정 결과는 [bench/RESULT.md](bench/RESULT.md), 방법은 [bench/README.md](bench/README.md) 에 있다.

---

## 2. 구조 & 타입

| 부채 | 증상 | 당시엔 왜      |
|---|---|------------|
| `HashMap<String,Object>` 만능주의<br>메서드 시그니처 84곳 / 49개 파일 | 어떤 키가 들어있는지 코드로 알 수 없다. 오타가 런타임까지 안 드러난다 | 개념 부재 / 관행 |
| 너무 많이 아는 Controller | 레이어를 나눠놨는데 `HashMap` 이 관통해서 경계가 무의미하다 | 개념 부재      |
| 트랜잭션 경계가 메서드명 규칙<br>`save*` `insert*` `update*` `delete*` `*Tx` | 이름을 잘못 지으면 조용히 트랜잭션이 빠진다. 컴파일도 테스트도 안 잡는다 | 관행         |
| SQL XML 에 섞인 로직 | `<if>` `<choose>` 가 쌓여 쿼리인지 분기인지 구분이 안 된다 | 관행         |
| 화면 1회 = 쿼리 2회 | 목록 화면이 `selectPrdList` 와 `selectPrdListCnt` 를 따로 부른다 | 개념 부재      |

`HashMap` 개수는 이렇게 셌다. 기준에 따라 값이 크게 달라져서 명령을 같이 적어둔다.

```bash
# 메서드 시그니처에 등장 : 84
find src/main/java -name '*.java' -exec \
  grep -hE '^[[:space:]]*(public|private|protected).*HashMap<String, ?Object>' {} + | wc -l

# HashMap 을 쓰는 파일 : 49
find src/main/java -name '*.java' -exec grep -l 'HashMap' {} + | wc -l
```

타입 표기 자체는 491번, `HashMap` 이 나오는 줄은 380줄이다.
레이어를 관통한다는 이야기에는 시그니처 수가 맞아서 그 값을 썼다.

---

## 3. 동시성

| 부채 | 증상 | 당시엔 왜      |
|---|---|------------|
| `MAX + 1` 채번<br>`selectMaxMbrNo` · `selectMaxPrdCd` · `selectMaxCatCd` | 동시에 두 건이 들어오면 같은 번호를 받는다 | 개념 부재 / 관행 |
| 날짜 + 난수로 만드는 주문번호<br>`NumberUtil` | 충돌 가능성이 남아 있고, 충돌하면 그대로 실패한다 | 개념 부재 / 관행 |
| 원자적이지 않은 재고 차감<br>조회 → 판단 → 차감이 분리, `UPDATE` 에 재고 조건 없음 | 동시에 주문하면 재고보다 많이 팔린다. 품절 전환도 안 걸린다 | 개념 부재      |
| 화면에만 있는 중복 주문 방어 | 서버는 같은 주문이 두 번 들어와도 그대로 받는다. 카드결제는 PG 승인이 두 번 난 뒤에야 DB 제약에 걸린다 | 관행         |

중복 주문은 결이 조금 다르다. 결제 버튼을 누르면 로딩 레이어가 화면을 덮고
응답이 올 때까지 클릭을 막는다. 실질적인 방어는 이 레이어다. 서버에는 같은 주문이
두 번 들어오는 것을 막는 장치가 없다.

결제수단에 따라 그다음이 갈린다. 카드결제는 결제 준비 단계에서 주문번호를 미리 받아
임시 주문에 담아두고, 무통장입금은 그 단계 없이 바로 확정한다. 같은 주문을 두 번
보내봤다.

| | 결과 | 왜 |
|---|---|---|
| 무통장입금 | 주문 2건이 그대로 생성됐다 | 요청마다 새 주문번호를 만들어 충돌할 것이 없다 |
| 카드결제 (재전송) | `주문 정보를 찾을 수 없습니다` | 앞선 결제가 임시 주문을 지웠다 |
| 카드결제 (동시) | 1건 성공, 1건 `시스템 오류` | 같은 주문번호로 둘 다 들어가 PK 가 충돌했다 |

카드결제가 막히는 것은 방어를 넣어서가 아니다. 임시 주문을 지우는 동작이 어쩌다
일회용 표 노릇을 하고, 그것도 뚫리면 마지막에 PK 제약이 걸리는 것뿐이다.
사용자에게는 `시스템 오류가 발생했습니다` 로 나간다.

그리고 PK 에 걸리는 시점이 너무 늦다. PG 승인은 주문 INSERT 보다 앞에 있어서,
충돌이 드러날 때는 이미 승인이 두 번 나 있다.

```
[MockPG] 승인 완료 - ORD...3822  승인번호:PG...6752
[MockPG] 승인 완료 - ORD...3822  승인번호:PG...5813   ← 같은 주문번호로 두 번
insertOrderMst PG...5813  → 성공
insertOrderMst PG...6752  → Duplicate entry 'ORD...3822' for key 'PRIMARY'
[MockPG] 취소 완료 - ORD...3822  승인번호:PG...6752   ← 승인을 되돌린다
```

여기서 롤백되는 것은 DB 뿐이다. 승인은 PG 쪽에 있으므로 따로 되돌려야 한다.
그래서 승인 이후 구간을 `try` 로 감싸고, 실패하면 `pgApiUtil.cancel()` 로 승인을 취소한 뒤
예외를 다시 던져 DB 를 롤백시킨다. 위 로그의 마지막 줄이 그 취소다.

이 보정이 없으면 승인번호 `PG...6752` 는 주문 없이 PG 에만 남는다.
돈은 움직였는데 대응하는 주문이 우리 쪽에 없는 상태다.

### 보정도 실패하면 거기서 끝난다

취소 요청 자체가 실패하는 경우를 넣고 다시 돌려봤다.

```
[MockPG] 취소 거절 - ORD...3602  승인번호:PG...1581
PG 취소 응답 - 결과:9002
ERROR PG 승인 취소 실패 - 주문번호:ORD...3602 승인번호:PG...1581 (승인만 남았으므로 수동 확인이 필요하다)
```

**로그 한 줄이 전부다.** 재시도도, 실패를 적어두는 테이블도, 알림도 없다.
누군가 로그를 뒤져 찾아내지 않으면 그 승인은 그대로 남는다.

정리하면 중복 주문을 막는 것은 화면 레이어뿐이고, 서버에서 걸리는 것은 PK 제약이다.
그 늦은 시점을 승인 취소가 받아내지만, 취소가 실패하는 순간 기댈 곳이 없어진다.

재현했다 ① — 재고 5개짜리 상품에 20명이 동시에 주문하면 20건이 전부 성공한다.

```
팔려야 할 수량 : 5 개     실제 주문 성공 : 20 건
실제 차감      : 9 회     남은 재고      : -4
```

**성공 건수(20)와 차감 횟수(9)가 다른 것이 요점이다.** 두 단계로 무너진다.

1. 조회와 차감이 분리돼 있고 차감 `UPDATE` 에 재고 조건이 없어 재고가 0 아래로 내려간다
2. 0 이하가 되면 `if (salePssbQty > 0)` 가 거짓이 되어 차감이 아예 멈춘다
   — 이 코드에서 `0` 은 "재고 미관리(무제한)" 를 뜻하기 때문이다

재고가 음수가 되는 데서 끝나지 않는다. 성공한 20건 가운데 11건은
재고를 건드리지도 못한 채 통과했다.

재현 : `./repro/oversell.sh 5 20`

> 재고가 음수로 내려가는 것은 이 저장소가 차감 방식이기 때문이다.
> 재고를 컬럼에서 빼지 않고 주문을 그때그때 합산하는 방식이었다면 음수는 나오지 않고,
> 대신 집계값이 한도를 넘는 형태로 나타난다. 어느 쪽이든 초과 판매라는 결과는 같다.
> 조회와 갱신 사이를 잠그지 않는다는 것이 원인이고, 방식은 그 원인이 드러나는 모양만 바꾼다.

재현했다 ② — 회원가입 20건을 동시에 넣으면 14건이 실패한다.
아이디는 전부 다른데도 그렇다 — 같은 순간에 `MAX` 를 읽은 요청들이
같은 회원번호를 만들고, PK 충돌로 INSERT 가 깨진다.

```
가입 시도 : 20 건      실제 생성 : 6 명      실패 : 14 건
```

재현 : `./repro/concurrent-signup.sh 20` ([repro/](repro/))

---

## 4. 외부 연동

| 부채 | 증상 | 당시엔 왜      |
|---|---|------------|
| 주문 트랜잭션 안의 PG 동기 호출 | 결제가 늦어지면 DB 트랜잭션이 그만큼 열려 있다 | 개념 부재 / 관행 |
| 타임아웃 없음<br>`pg.api.timeout=0` | 응답이 안 오면 무한정 기다린다 | 개념 부재      |
| 로그로 끝나는 승인 취소 실패 | 취소가 실패하면 승인만 남는다. 재시도도 기록도 없어 사람이 찾아내야 한다 | 개념 부재      |
| 폴백 없음 | 실패하면 그대로 실패다 | 개념 부재      |

PG 를 8초 지연시키고 주문 8건을 동시에 넣으면,
결제와 무관한 상품 목록이 0.057초 → 6.12초 가 된다. 107배다.

```
1. 지연 없음     상품 목록  0.057s
2. PG 8초 지연   상품 목록  6.116s   ← 결제 화면이 아니다
3. 지연 해제     상품 목록  0.060s
```

커넥션 풀 8개가 전부 PG 응답을 기다리는 주문 스레드에 묶이기 때문이다.

재현 : `./repro/pg-delay.sh` ([repro/](repro/))

---

## 5. 보안

| 부채 | 증상                                                                | 당시엔 왜      |
|---|-------------------------------------------------------------------|------------|
| CSRF 토큰 없음 | 상태를 바꾸는 요청을 외부에서 그대로 만들 수 있다                                      | 개념 부재      |
| 관행에 기댄 `pageSize` 방어<br>`PagingUtil` | 유틸 자체에는 상한이 없다. 지금은 컨트롤러 6곳이 전부 값을 덮어써서 막히지만, 한 곳만 빠뜨리면 `pageSize=999999` 로 전량 조회된다 | 개념 부재      |
| 접속정보를 파일에 직접 둔다 | 환경을 나누는 장치가 없다. 운영 값으로 바꾸려면 파일을 고쳐 다시 빌드해야 하고, 그 파일이 저장소에 함께 올라간다 | 개념 부재 / 관행 |

접속정보를 파일에 그대로 둔 것은 사내 SVN 을 쓰던 시절의 관행이다.
저장소가 회사 안에만 있었으니 "외부에 공개된다" 는 상황이 아예 없었다.
저장소를 볼 수 있는 사람은 대개 서버에도 접근할 수 있었다.
그래서 파일에 적어두는 것과 따로 관리하는 것 사이에 차이를 느낄 일이 없었다.

git 으로 옮겨오면서 전제가 바뀌었다. 저장소가 공개될 수 있고,
한 번 커밋한 값은 히스토리에 남아 나중에 지워도 따라온다.
코드는 그대로인데 위험만 달라진 경우다.

> 이 저장소의 `commerce` 는 로컬 도커용 개발 계정이라 진짜 비밀이 아니다.
> `docker-compose.yml` 과 값이 맞아야 앱이 붙으므로 여기 있는 것이 맞다.
> 재현한 것은 값이 아니라 환경을 나누지 않는 구조다.

---

## 6. 에러 처리 · 운영

| 부채 | 증상 | 당시엔 왜      |
|---|---|------------|
| `e.printStackTrace()`<br>7곳 / 5파일 | 장애가 나면 콘솔에만 남는다. 어떤 요청이었는지, 누구였는지 알 수 없다 | 개념 부재      |
| 삼켜지는 예외 | `catch` 하고 기본값을 리턴한다. 실패한 줄 모른 채 넘어간다 | 개념 부재      |
| WAR 통짜 배포 | 한 줄을 고쳐도 전체를 다시 올린다. 배포 = 전체 중단 | 개념 부재 / 관행 |
| WAS 메모리에 둔 세션 | 서버를 늘리면 sticky session 없이는 로그인이 풀린다 | 개념 부재      |
| 역할이 섞인 설정 파일<br>`message-common.properties` | 화면 문구와 외부 연동 설정(`pg.api.url`, `pg.api.timeout`)이 한 파일에 있다. 문구를 고치러 열었다가 연동 설정을 건드릴 수 있다 | 관행         |

---

## 7. 지우지 않은 것들

"안 쓰는 것은 삭제하지 않는다" 는 SI/SM 현장의 관행 그대로다.
지우면 뭐가 터질지 모르니 그냥 둔다.

| 부채 | 증상 | 당시엔 왜 |
|---|---|---|
| 미사용 SQL·메서드<br>`selectMaxOrdNo` · `deleteCartByOrdNoTx` | 선언과 XML 은 남아 있는데 부르는 곳이 없다 | 관행 |
| 죽은 설정<br>`<mvc:resources mapping="/prdimg/**">` | 실제 업로드 경로는 `/upload` 다. 아무 역할도 안 한다 | 관행 |
| 미사용 컬럼<br>`TB_PRD_PRC.CPN_NO` · `LOGIN_FAIL_CNT` | 컬럼만 있고 로직이 없다 | 관행 |
| 컬럼명 오타<br>`RECIVER_NM` (`RECEIVER`) | 한 번 박히면 안 고친다. 고치려면 전 계층을 손대야 한다 | 관행 |
| 코드 밖에 있는 용어사전<br>[GLOSSARY.md](GLOSSARY.md) | 축약 규칙을 강제하는 장치가 없다. 사전과 코드가 갈라져도 아무도 알려주지 않는다 | 관행 |

---

## ※ 이건 재현이 아니다

아래 둘은 원본에 없던 것들이다. 이 저장소가 만들었다. 출처를 밝혀둔다.

| | 원본 | 이 저장소 | 왜 |
|---|---|---|---|
| FO 상품 검색 | 외부 검색솔루션 | DB `LIKE '%x%'` | 검색을 범위에서 빼면서 DB 조회로 대체했다 |
| 이미지 저장 | CDN | 웹앱 로컬 디스크 | 외부 의존을 줄이려는 단순화 |

> 관리자 검색은 다르다. 원본 관리자도 DB `LIKE '%키워드%'` 였고,
> 조회 쿼리 대부분이 그 형태였다. 그쪽은 정확한 재현이다.

성능 특성은 실제로 측정했으므로 일반적인 교훈으로는 유효하지만,
"당시 시스템이 이랬다" 로 읽으면 사실과 다르다.

이미지 저장은 직접 겪어볼 수 있다 — `ServletContext.getRealPath("/upload")` 로
웹앱 안에 저장하는데, `tomcat7:run-war` 로 띄우면 그 경로가 빌드 산출물인 `target/` 이다.
`mvn clean` 한 번이면 파일만 사라지고 DB 에는 경로가 남아 깨진 이미지가 된다.

---

## 재현의 한계

먼저 인정해둔다.

- 도메인이 작다. 상품 / 주문에만 집중했다. 검색 / 쿠폰 / 협력사는 부채를 보여주는 데 필요하지 않아 뺐다. 실제 커머스의 복잡도와는 거리가 있다
- 무거운 조회 화면이 없다. 통계나 엑셀 다운로드처럼 데이터가 쌓일수록 버거워지는 화면은 만들지 않았다
- SQL 이 전부 애플리케이션 쪽에 있다. 뷰 / 프로시저 / 함수 안으로 로직이 들어앉는
  영역은 이 저장소에 없다
